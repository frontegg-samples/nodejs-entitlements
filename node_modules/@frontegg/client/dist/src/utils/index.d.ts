export interface RetryOptions {
    numberOfTries: number;
    secondsDelayRange: {
        min: number;
        max: number;
    };
}
export declare const retry: (func: () => Promise<unknown> | unknown, { numberOfTries, secondsDelayRange }: RetryOptions) => any;
