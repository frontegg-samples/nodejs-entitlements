"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapFromTuple = void 0;
function mapFromTuple(tuple) {
    const [, /* featureKey */ on /* type */, , defaultTreatment, offTreatment, rules] = tuple;
    return {
        on,
        defaultTreatment: defaultTreatment,
        offTreatment: offTreatment,
        rules: rules.map(mapRule),
    };
}
exports.mapFromTuple = mapFromTuple;
function mapRule(rule) {
    return {
        treatment: rule.treatment,
        conditions: rule.conditions.map((condition) => {
            return {
                op: condition.op,
                value: condition.value,
                negate: condition.negate,
                attribute: condition.attribute,
            };
        }),
        conditionLogic: rule.conditionLogic,
    };
}
//# sourceMappingURL=feature-flag-tuple.mapper.js.map