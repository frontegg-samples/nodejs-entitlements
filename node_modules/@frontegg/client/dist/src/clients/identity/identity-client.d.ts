import { AuthHeaderType, IUser, IValidateTokenOptions, TEntity } from './types';
import { IFronteggContext } from '../../components/frontegg-context/types';
export declare class IdentityClient {
    static getInstance(): IdentityClient;
    private static instance;
    private publicKey;
    private readonly context;
    constructor(context: IFronteggContext);
    getPublicKey(): Promise<string>;
    validateToken(token: string, options?: IValidateTokenOptions, type?: AuthHeaderType): Promise<TEntity>;
    validateIdentityOnToken(token: string, options?: IValidateTokenOptions): Promise<IUser>;
    private extractTokenCredentials;
    private fetchPublicKey;
}
