import { IEntityWithRoles, IUserAccessToken } from '../../../types';
import { AccessTokenService } from './access-token.service';
import { HttpClient } from '../../../../http';
export declare class UserAccessTokenService extends AccessTokenService<IUserAccessToken> {
    readonly httpClient: HttpClient;
    constructor(httpClient: HttpClient);
    getEntityFromIdentity(entity: IUserAccessToken): Promise<IEntityWithRoles>;
    getActiveAccessTokenIdsFromIdentity(): Promise<string[]>;
}
