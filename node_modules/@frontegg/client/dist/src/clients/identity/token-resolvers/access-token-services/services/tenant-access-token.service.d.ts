import { IEntityWithRoles, ITenantAccessToken } from '../../../types';
import { AccessTokenService } from './access-token.service';
import { HttpClient } from '../../../../http';
export declare class TenantAccessTokenService extends AccessTokenService<ITenantAccessToken> {
    readonly httpClient: HttpClient;
    constructor(httpClient: HttpClient);
    getEntityFromIdentity(entity: ITenantAccessToken): Promise<IEntityWithRoles>;
    getActiveAccessTokenIdsFromIdentity(): Promise<string[]>;
}
