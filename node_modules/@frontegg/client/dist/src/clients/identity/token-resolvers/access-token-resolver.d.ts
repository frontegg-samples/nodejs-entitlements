import { IAccessToken, IEntityWithRoles, IValidateTokenOptions, TEntityWithRoles } from '../types';
import { TokenResolver } from './token-resolver';
export declare class AccessTokenResolver extends TokenResolver<IAccessToken> {
    private authenticator;
    private httpClient;
    private accessTokenServices;
    constructor();
    validateToken(token: string, publicKey: string, options?: IValidateTokenOptions): Promise<IAccessToken | TEntityWithRoles<IAccessToken>>;
    init(): Promise<void>;
    protected getEntity(entity: IAccessToken): Promise<IEntityWithRoles>;
    private getActiveAccessTokenIds;
    private getAccessTokenService;
    private initAccessTokenServices;
}
