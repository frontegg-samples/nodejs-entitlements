import { IEntityWithRoles, IValidateTokenOptions } from '../types';
import { TokenResolver } from './token-resolver';
export declare class AuthorizationJWTResolver extends TokenResolver<IEntityWithRoles> {
    constructor();
    validateToken(token: string, publicKey: string, options?: IValidateTokenOptions): Promise<IEntityWithRoles>;
    protected getEntity(entity: IEntityWithRoles): Promise<IEntityWithRoles>;
}
