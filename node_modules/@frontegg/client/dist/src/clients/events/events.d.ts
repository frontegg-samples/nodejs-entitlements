import { FronteggAuthenticator } from '../../authenticator';
import { EventStatus, EventId, EventTrigger } from './types';
export declare class EventsClient {
    private readonly authenticator;
    constructor(authenticator: FronteggAuthenticator);
    send(tenantId: string, ev: EventTrigger): Promise<EventId>;
    getStatus(eventId: EventId): Promise<EventStatus>;
    private validateInput;
    private sendEventRequest;
    private sendEventStatusRequest;
}
